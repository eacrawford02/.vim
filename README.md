The packages included with this Vim configuration, located in ".vim/pack", are
Git submodules. To fetch all the data for each plugin, pass the
`--recurse-submodules` option to `git clone`. If the repository has already been
cloned, simply run `git submodule update --init --recursive` to initialize,
fetch, and checkout any nested submodules.
